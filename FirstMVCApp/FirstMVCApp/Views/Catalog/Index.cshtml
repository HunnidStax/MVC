@{
    ViewData["Title"] = "Catalog list";
}

<div>
    <h1>Catalog list</h1>
    <hr>
    @{
        Catalog catalog = (Catalog)@ViewData["Catalog"];
        if (catalog != null && catalog?.ProductsList != null)
        {
            @foreach (var item in catalog.ProductsList)
            {
                <div>Id: @item.Id; Name: @item.Name; <img src="@item.Img" alt="product picture" width="100"/></div>
            }
        }
    }
</div>