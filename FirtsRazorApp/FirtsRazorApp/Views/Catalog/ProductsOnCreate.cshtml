@{
    ViewData["Title"] = "Catalog list";
}

<div>
    <h1>Catalog list</h1>
    <hr>
    @{
        Catalog catalog = (Catalog)@ViewData["Catalog"];
        if (catalog != null && catalog?.ProductList != null)
        {
            @foreach (var item in catalog.ProductList)
            {
                <div>Id: @item.Id; Name: @item.Name; <img src="@item.Img" alt="prod picture" width="100"/></div>
            }
        }
    }
</div>